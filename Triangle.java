package kr.dv.pratic;

public class Triangle {
	/*
	//x 밑변
	//y 높이 
     private int x,y;
     
     //생상자
     //public Person() {} <-- 다른 생성자사 생성되면 삭제됨.
     //접근지시자 객체이름(매게변수) {}
     public Triangle(int x,  int y) {
    	 this.x = x;
    	 this.y = y;
     }
     
     //삼각형의 넓이를 출력하는 함수(메서드)
     public void printArea() {
    	 System.out.println("삼각형의 널이는 : "+ (x * y / 2));
     }
     */
 }
